#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a = 2.0;
    double b = 14.36;
    double y;

    double numerator = 8.15 * cbrt(b) * log(a); 
    double denominator = 24.38 * cos(b) * (exp(a) - pow(a, a));

    y = numerator / denominator;

    cout << "y = " << y << endl;

    return 0;
}


#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a=2.0;
    double b=200.0;
    double y;
    
    double numerator=11.21*(1+log(b/a));
    double denominator=1-sin((a/b)*M_PI)*cos((a/b)*M_PI);
    
    y=pow(numerator/denominator,2.0/3.0);
    
    cout<<"y="<<y<<endl;
    
    return 0;
}
